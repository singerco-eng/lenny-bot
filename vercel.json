{
  "buildCommand": "npm --prefix admin run build",
  "installCommand": "npm --prefix admin install",
  "outputDirectory": "admin/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

